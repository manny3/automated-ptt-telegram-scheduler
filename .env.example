# 環境變數範本檔案
# 複製此檔案為 .env.local 並填入實際值

# === Google Cloud 設定 ===
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json

# === Secret Manager 設定 ===
TELEGRAM_BOT_TOKEN_SECRET_NAME=telegram-bot-token

# === Firestore 設定 ===
# 本地開發時使用模擬器
FIRESTORE_EMULATOR_HOST=localhost:8080
# 生產環境時留空，使用預設連接

# === Redis 設定 (可選) ===
REDIS_URL=redis://localhost:6379

# === Next.js 設定 ===
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret

# === 應用程式設定 ===
NODE_ENV=development
PORT=3000

# === 監控和日誌 ===
# 停用 Next.js 遙測
NEXT_TELEMETRY_DISABLED=1

# === 開發工具 ===
# 啟用 React 嚴格模式
REACT_STRICT_MODE=true

# === 部署設定 ===
# Cloud Run 設定
CLOUD_RUN_SERVICE_NAME=ptt-telegram-scheduler
CLOUD_RUN_REGION=us-central1

# Cloud Function 設定
CLOUD_FUNCTION_NAME=ptt-scraper
CLOUD_FUNCTION_REGION=us-central1

# Cloud Scheduler 設定
SCHEDULER_JOB_NAME=ptt-telegram-scheduler
SCHEDULER_REGION=us-central1

# === 功能開關 ===
# 啟用除錯模式
DEBUG=false

# 啟用詳細日誌
VERBOSE_LOGGING=false

# === 安全設定 ===
# CORS 允許的來源 (生產環境中設定)
ALLOWED_ORIGINS=http://localhost:3000,https://your-domain.com

# === 效能設定 ===
# 最大並發請求數
MAX_CONCURRENT_REQUESTS=100

# 請求逾時 (毫秒)
REQUEST_TIMEOUT=30000